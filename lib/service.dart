class CardService {
  String title;
  String subtitle;
  String description;
  bool isHovered;
  CardService({
    required this.title,
    required this.subtitle,
    required this.description,
    required this.isHovered,
  });
}
